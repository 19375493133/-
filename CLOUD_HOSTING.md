# 云服务器选型：把「大学课堂听课助手」装到云上

> 本文只做调研，没有购买、注册或部署任何东西。
> 标注「已核实」的条目是我这次打开官网/官方文档确认过的；标注「需自行核实」的请以你下单时的页面为准。

## 零、先澄清一个概念

「云服务器」本身**没有开源的**——服务器是租来的硬件。市面上常说「开源的云」其实指两类东西：

1. **开源的服务器管理面板 / 自建 PaaS**（Coolify、Dokploy、1Panel 等）：你自己租一台服务器，
   装上它，就能像 Vercel 那样用 Git 一键部署、自动配 HTTPS。**开源的是软件，不是机器。**
2. **免费的云服务器**：Oracle Cloud、各家新用户试用等，机器不要钱但有限制。

下面两条路都给你，最后给一个针对本项目的推荐组合。

## 一、我们这个项目对服务器的要求

先算清楚需要多少资源，选型才不会瞎买：

| 组件 | 资源画像 |
| --- | --- |
| FunASR `paraformer-zh-streaming`（PyTorch CPU 推理） | 模型约 880MB，加载后常驻内存约 1.5-2GB；本机实测 rtf≈0.37（即 1 秒音频花 0.37 秒算完），**CPU 越强越稳** |
| FastAPI 后端 + SQLite | 内存几百 MB，几乎不吃 CPU |
| Next.js 前端 | 几百 MB |
| 音频文件存储 | 一节课 45 分钟、48kbps ≈ 16MB；一学期几百节也就几 GB |

**结论：2 核 4GB 起步比较舒服，2 核 2GB 会比较紧（要开 swap），4 核 8GB 就很宽裕。**
带宽要求不高（音频上传是小流量），3-5Mbps 足够；真正吃紧的是**内存**和**单核性能**。

另外三个硬需求：

- 要能跑**常驻进程 + WebSocket 长连接**；
- 要有**持久磁盘**放音频（Serverless 按需函数不适合）；
- 国内长期用的话要能**域名备案**（小程序必须，网页版想用 80/443 也建议）。

## 二、免费云服务器（0 元档）

### 1. Oracle Cloud Always Free（已核实，2026-09 官网）

官网当前写的是：

- **AMD 实例**：2 台，每台 1/8 OCPU + 1GB 内存；
- **ARM Ampere A1 实例**：**每月 1,500 OCPU 小时 + 9,000 GB 小时**，可用 1 台或 2 台，
  换算下来大约等于 **2 核 12GB 一直开着**（或者 4 核 24GB 只开半个月）。

> 注意：网上大量教程还写着「4 核 24GB 永久免费」，那是旧额度，官网已经改了，别按那个规划。

优点：额度大，能跑 FunASR；缺点：注册要信用卡验证、热门区域经常「Out of capacity」开不出机器、
国内访问延迟高（100-300ms）、有闲置回收和封号风险。**适合折腾型用户试水，不适合当唯一生产环境。**

### 2. 国内厂商新用户试用（已核实入口）

- 阿里云免费试用页：<https://free.aliyun.com/>，页面显示「175 款试用产品」，含轻量应用服务器/ECS；
  同一页面还有 **ECS「99 计划」：2 核 2G 包年**的长期低价套餐（页面价格为动态渲染，下单时确认）。
- 腾讯云轻量应用服务器：页面写明「**新用户免费体验 1 个月**（个企同享）」。

适合「先验证能不能跑起来」，到期会自动停服或转按量计费，**记得设提醒**。

### 3. 模型/应用托管平台（不是服务器，但能省事）

- **ModelScope 创空间**（已核实）：国内魔搭社区的 AI 应用托管，「基于社区模型用 Gradio 等快速搭建 AI 应用」，
  Notebook 也给免费 CPU/GPU 时长。可以把 FunASR 单独放上去，用 HTTP/WS 调用。
- **Hugging Face Spaces**：同类平台，免费 CPU 实例；但**我这台机器 DNS 解析不了 huggingface.co**，
  国内访问也不稳定，只能作为备选。
- Render / Railway / Fly.io：免费额度小、会自动休眠，**跑不动常驻 ASR**，不建议放主服务。

## 三、便宜付费 VPS（长期用推荐）

### 腾讯云轻量应用服务器（已核实官网价格）

| 套餐 | 配置 | 价格 |
| --- | --- | --- |
| 入门型 | 2 核 2G / 40G SSD / 3Mbps / 200G 流量 | 45 元/月，459 元/年 |
| 入门型（基础） | 2 核 2G / 50G / 4Mbps / 300G | 50 元/月，510 元/年 |
| **通用型（进阶）** | **2 核 4G / 100G / 7Mbps / 1000G** | **100 元/月，1020 元/年** |
| 通用型（专业） | 4 核 8G / 180G / 12Mbps | 245 元/月，2499 元/年 |

选 **2 核 4G** 这一档就够跑「后端 + FunASR + 前端」全套；国内节点延迟低，域名能备案。
学生认证通常还有更便宜的校园机。

### 海外便宜 VPS（需自行核实价格）

Hetzner、Vultr、DigitalOcean、RackNerd 之类，2 核 4G 大约每月 5-10 美元，配置给得足，
但**国内访问慢**，而且**小程序不能直连未备案的境外域名**——所以如果以后要做微信小程序，还是国内机器更省事。

## 四、开源自建云平台（在服务器上装这个，部署会轻松很多）

### Coolify（已核实官网）

- 自我定位：「开源的、可自托管的 Vercel / Heroku / Netlify / Railway 替代品」；
- 支持任意服务器（自有服务器、VPS、树莓派、EC2、DigitalOcean…），只要 SSH 能连；
- 支持任意 Docker 化的服务，内置 280+ 一键服务；
- **自动申请和续期 Let's Encrypt HTTPS 证书**、Git 推送部署、自动数据库备份、API/Webhook；
- 官网显示自托管实例数 **508,262+**，社区活跃。

### Dokploy（已核实官网）

- 开源、自托管；一行命令安装：`curl -sSL https://dokploy.com/install.sh | sh`；
- **原生 Docker Compose 支持**（我们正好是「api + funasr + web」这种多容器项目）；
- 多服务器管理、数据库备份、Docker Swarm 集群、实时监控告警、API/CLI，页面也提到自带 MCP 接入。

### 其他（按需了解）

- **1Panel**：国产开源面板（本次因 `1panel.cn` DNS 解析失败未能核实，需自行确认）。
- Dokku / CapRover / Kamal：更轻，但功能比上面两个少。
- k3s / KubeSphere / OpenStack：企业级/太重，个人项目不建议。

**给我们的建议：腾讯云轻量 2 核 4G + Coolify（或 Dokploy）+ Docker Compose。**
之后改代码只要 `git push`，HTTPS、证书续期、容器重启这些都不用自己管。

## 五、推荐的三种组合

| 目标 | 组合 | 花费 | 说明 |
| --- | --- | --- | --- |
| 先免费试通 | Oracle Always Free（ARM 2核12G）+ Docker Compose | 0 元 | 注册难、延迟高；练手可以，别当长期方案 |
| **长期自己用（推荐）** | 腾讯云轻量 2核4G + Coolify | 约 100 元/月 或 1020 元/年 | 国内低延迟、能备案，一台机器跑全部 |
| 以后要做微信小程序 | 同上 + 域名备案，或用微信云托管 | 域名几十元/年 + 备案（1-3 周） | 小程序要求 HTTPS + ICP 备案域名，见 `WECHAT_MINIPROGRAM.md` |

## 六、部署形态草图（Docker Compose）

```yaml
# docker-compose.yml（示意，真正上线前要再调）
services:
  api:                     # FastAPI 后端
    build: ./backend
    volumes: ["./data:/app/data"]
    restart: unless-stopped
  funasr:                  # FunASR 流式转写
    build: ./funasr_backend
    restart: unless-stopped
  web:                     # Next.js 前端（同源反代 /api，已实现）
    build: ./frontend
    environment:
      NEXT_PUBLIC_API_BASE_URL: "/"
      NEXT_PUBLIC_FUNASR_API_URL: "https://你的域名"
    restart: unless-stopped
  caddy:                   # 自动 HTTPS
    image: caddy:2
    ports: ["80:80", "443:443"]
    volumes: ["./Caddyfile:/etc/caddy/Caddyfile", "caddy_data:/data"]
volumes:
  caddy_data:
```

几个要点：

- 本项目的公网版前端已经支持「页面与 `/api` 同源」，所以**只需要一个域名**，不用给后端单独配域名和 CORS；
- FunASR 的 WebSocket 走同一个域名下的 `/funasr` 路径，用 Caddy 反代即可（`wss` 也走 443）；
- 首次启动 FunASR 要下载 880MB 模型，建议在镜像构建阶段或挂载数据卷预热，别每次重启都下。

## 七、踩坑清单

1. **内存**：2G 机器跑 PyTorch 很容易 OOM，务必加 2G swap，或直接上 4G。
2. **ARM 兼容**：Oracle 免费机是 ARM64，装 torch/funasr 前先确认有 aarch64 轮子；出问题就换 x86 机器。
3. **备案**：国内机器用 80/443 且绑域名，需要 ICP 备案；只给自己测试可以先用 IP + 非 443 端口。
4. **免费转付费**：试用到期可能自动转按量计费，阿里云页面写明「非预期费用可退」，但仍建议设置消费提醒。
5. **闲置回收**：Oracle 对长期闲置实例有回收策略，别把它当唯一备份。
6. **数据备份**：音频和数据库放在服务器上，记得定期备份（Coolify/Dokploy 都自带 S3 备份能力）。
7. **模型体积**：FunASR 模型 + 依赖镜像体积很大，选机器时把系统盘算够（至少 40G）。

## 八、下一步可以怎么走

如果你想让我继续，我建议按这个顺序：

1. 你先把服务器开出来（推荐腾讯云轻量 2核4G 或先用免费试用），把 SSH 信息准备好；
2. 我这边把仓库改成**一键 Docker 部署**（写 `Dockerfile`、`docker-compose.yml`、`Caddyfile`、部署脚本），
   在本地先验证过再交给你；
3. 你在服务器上跑一条命令把服务拉起来，我再远程核对接口是否正常。

需要我先做第 2 步（写 Docker 部署文件）吗？这一步不需要你有服务器，我可以先在本地把镜像构建和运行验证完。

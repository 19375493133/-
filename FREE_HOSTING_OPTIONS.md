# 不用自己电脑常开：把课堂助手放到公网上的几种方案

> 调研日期：2026-09-20。**本文只是调研，没有部署任何东西，也没有改代码。**
> 星标/额度类信息变化很快，注册前请以平台当时的页面为准。

## 一、先给结论

现在的痛点是：**服务跑在你电脑上 + 隧道是临时的**，所以电脑一关、会话一结束，网址就失效。
要摆脱它，本质只有两条路：

1. **把「重」的部分换掉**（本地 FunASR 模型 → 云端 ASR 接口），后端变轻，随便找个免费平台就能放；
2. **把服务器搬到云上**（容器平台或云主机），功能完全不变，但要接受额度/费用和运维。

另外有个重要前提：**海外免费平台在国内访问不稳定**。我这次从你这台电脑实测，
`huggingface.co`（HF Spaces）和 `render.com`（Render 免费实例）都是**直接超时打不开**，
所以它们虽然是"免费"，对你的手机和同学来说大概率也不好用。

## 二、实测可达性（2026-09-20 从本机访问）

| 平台 | 结果 | 备注 |
| --- | --- | --- |
| huggingface.co（HF Spaces） | ❌ 连接超时 | 国内访问不稳定，免费 CPU 空间还会休眠 |
| render.com | ❌ 连接超时 | 免费实例也会休眠 |
| vercel.com | ✅ 可访问 | 官方文档可读，Hobby 免费版限制明确 |
| modelscope.cn（魔搭创空间） | ✅ 可访问 | 国内平台，有免费 CPU 空间与「魔粒」积分体系 |
| aliyun.com（函数计算 FC） | ✅ 可访问 | 国内 Serverless，有免费试用额度 |
| cloud.tencent.com（CloudBase/云托管） | ✅ 可访问 | 国内，微信生态打通 |
| siliconflow.cn（硅基流动） | ✅ 可访问 | 有语音识别 API（含开源 ASR 模型） |

## 三、四条可选路线

### 路线 1：纯静态前端 + 浏览器识别（0 服务器、0 成本）

- **怎么做**：前端部署到 Vercel / Netlify / Cloudflare Pages（都免费）；
  实时字幕用浏览器自带的 Web Speech（我们代码里已有 `webspeech` Provider）；
  会话数据存在浏览器本地（我们已有 localStorage 兜底逻辑）；重点提取/导图可以直接在浏览器里算，或让用户填自己的 LLM Key。
- **优点**：完全不需要服务器，网址固定不变，永远在线。
- **缺点**：没有 FunASR 的高精度中文识别；换手机/清缓存数据就没了；iOS 微信里 Web Speech 支持不稳。
- **要改的代码**：默认 Provider 改成 `webspeech`、把数据层固定走 localStore、`analyze/mindmap` 增加纯前端实现。

### 路线 2：前端 Vercel + 后端容器上「国内平台」（功能完整）

- **怎么做**：前端放 Vercel/Netlify 免费版；后端 FastAPI + FunASR 打成一个 Docker 镜像，
  部署到 **魔搭创空间 / 腾讯云 CloudBase 云托管 / 阿里云函数计算 FC** 这类国内平台。
- **优点**：功能跟现在一模一样（含 FunASR 流式字幕），国内访问快，前端完全免费。
- **缺点**：后端有免费额度/休眠限制（免费 CPU 空间通常闲着就休眠，冷启动要几十秒，
  FunASR 模型加载本身也要 10-20 秒）；容器要装 torch，镜像好几个 G，要确认平台上限。
- **要改的代码**：`funasr_backend` 补 Dockerfile（`whisper_backend` 已经有可参考）、
  长连接的地址通过环境变量指向云上、前端 `NEXT_PUBLIC_FUNASR_API_URL` 改成云地址。

### 路线 3：把 ASR 换成云 API（后端变轻，能放 Serverless）

- **怎么做**：不再在服务器上跑 FunASR，改成调用云端语音识别接口，
  例如[硅基流动](https://siliconflow.cn/)、阿里云百炼（Paraformer 系）、讯飞开放平台等，都有免费额度或极低单价。
- **优点**：后端从"几个 G 的模型"变成"几百 K 的转发服务"，可以放进 Vercel 的 Python 函数。
- **缺点**：**Vercel 的 Serverless 函数不支持 WebSocket 长连接**，
  所以实时字幕要么改成"录完再转"，要么换成浏览器识别；另外音频会上传到第三方（隐私要重新提示）。
- **要改的代码**：新增一个 `cloud_asr` Provider，替换 FunASR 那条链路。

### 路线 4：一台云主机（功能不变，最省心）

- **怎么做**：Oracle Cloud 永久免费 ARM 实例（4 核 24G）或国内云厂商的轻量应用服务器活动机（几十元/年），
  在上面跑我们这套 `start-all` + Docker，用 Caddy/Nginx 配域名和 HTTPS。
- **优点**：24 小时在线、功能完全不变、可以固定域名，再也不怕电脑关机。
- **缺点**：要自己运维（系统更新、进程守护）；Oracle 注册审核较麻烦且国内延迟高，
  国内活动机便宜但不是"永久免费"。

## 四、Vercel 免费版的硬限制（已核对官方文档）

因为 Vercel 是国内能正常访问、且免费额度对个人够用的平台，把它的限制列清楚：

| 项目 | Hobby（免费） |
| --- | --- |
| 函数最大执行时长 | **300 秒**（默认=上限） |
| 函数最大内存 | 2 GB |
| 函数包体积 | Python 500 MB（开通 large functions 可到 5 GB，Beta） |
| 静态文件上传 | 100 MB |
| 代理请求超时 | 120 秒 |
| 支持 FastAPI（ASGI） | ✅ 官方支持 |
| 支持 WebSocket 长连接 | ❌ 不支持 |

结论：**前端放 Vercel 很合适；后端放 Vercel 只能做"录完再转"，做不了实时字幕。**

## 五、对着我们现有代码，各路线要动什么

| 路线 | 要改的部分 | 大概工作量 |
| --- | --- | --- |
| 1 纯静态 | 默认 Provider、数据层固定本地、前端版 analyze/mindmap | 1-2 天 |
| 2 容器上云 | funasr_backend 的 Dockerfile、云平台配置、环境变量 | 2-4 天（+ 平台注册审核） |
| 3 云 ASR | 新增 cloud_asr Provider、隐私提示、去掉流式或改批处理 | 2-3 天 |
| 4 云主机 | 部署脚本 + 反向代理 + 域名 + 进程守护 | 1-2 天（+ 买机/注册） |

## 六、注册前要自己确认的几件事

1. **免费额度**：魔搭的"魔粒"、阿里云 FC 的免费试用、腾讯云 CloudBase 的免费额度，都会变，注册时看当天规则。
2. **实名/支付方式**：国内平台基本都要实名；Oracle 等海外平台通常要信用卡验证。
3. **休眠策略**：免费容器/空间"闲着就睡"是常态，冷启动可能要等 20-60 秒。
4. **带宽与体积**：FunASR 镜像好几个 G，确认平台对镜像大小和磁盘的限制。
5. **合规**：课堂录音涉及他人声音，放公网前要保证隐私提示、必要时征得同意（我们页面上已有提示）。

## 七、推荐方案（结论）

### 7.1 一句话结论

> 只要还是「本机跑服务 + 临时隧道」，这个地址就会反复失效（我们实测过：两个域名先后变成
> Cloudflare **530 Origin DNS error**，本机进程也被回收）。
>
> **要彻底解决，必须让后端常驻在云上。** 免费又想常驻，目前最现实的组合就是
> **路线 2（前端 Vercel + 后端容器上魔搭创空间）**；
> 如果能接受几十块钱，**路线 4（一台轻量云主机）体验最好、几乎不用改代码**。

### 7.2 四方案决策表

| 方案 | 花钱 | 地址是否固定 | 电脑要开着吗 | 功能会不会缩水 | 落地难度 | 数据会不会丢 |
| --- | --- | --- | --- | --- | --- | --- |
| 继续现在的「本机 + 临时隧道」 | 0 | ❌ 每次重启换域名 | ✅ 要 | 不缩水 | 最容易 | 不丢（在本地） |
| 路线 1 纯静态前端 | 0 | ✅ | ❌ | 缩水（无 FunASR 高精度、无服务端存档） | 低 | 换手机/清缓存就丢 |
| **路线 2 前端 Vercel + 后端容器** | 0（免费额度内） | ✅ | ❌ | 基本不缩水 | 中（要注册/实名） | ⚠️ 容器重启可能丢，除非挂持久存储 |
| 路线 3 云 ASR API | 0～低 | ✅ | ❌ | 只能"录完再转"（Vercel 不支持 WebSocket） | 中 | 看数据库选择 |
| **路线 4 一台云主机** | 约几十元/年（活动机）或 Oracle 永久免费 | ✅ | ❌ | **完全不缩水** | 中（要配一次） | 不丢（磁盘持久） |

### 7.3 分阶段建议

**阶段一（今天就能用，0 成本、0 风险）**：继续用双击 `go-online.cmd` 这套。
接受"地址每次会变"这个事实，把它当成"临时演示环境"用；地址失效就重跑一次。

**阶段二（想要固定地址，推荐）**：走路线 2。
需要你做两件事：注册魔搭（要实名）+ 注册 Vercel；之后建空间、上传、配环境变量都可以由我接手。
**这一步做完，手机地址就永久固定，电脑关机也不影响。**

**阶段三（想更省心、能花点小钱）**：买一台轻量云主机（或 Oracle 永久免费 ARM），
把现在这套原样搬上去。优点：功能 100% 一致、磁盘持久、没有休眠冷启动，一次配好长期不用管。

### 7.4 为什么不推荐另外几条

- **海外免费平台（HF Spaces / Render / Fly.io）**：本机实测直接超时，国内访问不稳定，放弃。
- **纯静态前端**：等于把 FunASR 换成浏览器识别，中文课堂识别质量会明显下降，而且录音记录只存在手机里。
- **Vercel 免费函数直接跑 FunASR**：函数不支持 WebSocket 长连接，实时字幕做不了；镜像体积也超免费上限。

## 八、验证方式

- 逐个访问平台官网/文档，记录可达性（HF、Render 实测超时；Vercel、魔搭、阿里云、腾讯云、硅基流动可访问）；
- Vercel 官方文档核对：Python/ASGI 支持、函数时长/内存/体积限制、静态上传限制；
- 额度与计费类信息只做定性描述，具体数字标注为"以注册时页面为准"。

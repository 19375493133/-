# Cloudflare Pages 专用：静态导出后，/sessions/<任意 id> 这些路径没有对应的 HTML，
# 统一代理到构建时生成的占位页；页面内部用 useParams() 读真实 URL，所以 id 不会丢。
#
# 注意顺序：Cloudflare 的 _redirects 规则「永远生效，不管静态资源是否匹配」，
# 所以 /sessions/new 这条正路必须写在通配规则前面，否则会被 :id 抢走。

/sessions/new          /sessions/new/index.html 200
/sessions/:id/record   /sessions/_placeholder/record/index.html 200
/sessions/:id/mindmap  /sessions/_placeholder/mindmap/index.html 200
/sessions/:id          /sessions/_placeholder/index.html 200
